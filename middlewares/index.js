const auth = require("./auth");
const upload = require("./upload");
const controlWrapper = require("./controlWrapper");
const productsSchemaValidation = require("./productsSchemaValidation");

module.exports = {
  auth,
  upload,
  controlWrapper,
  productsSchemaValidation,
};
